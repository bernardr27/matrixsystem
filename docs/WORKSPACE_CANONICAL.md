# Workspace Canonical IDs

Use these workspace names in all npm commands and automation scripts:

- `citadel`
- `ghost-command`
- `nexus`
- `reflect`
- `rocket-command-pro`

Notes:

- The folder is `apps/rocket-command`, but the workspace name is `rocket-command-pro`.
- Always prefer workspace-based scripts over path-based assumptions.
